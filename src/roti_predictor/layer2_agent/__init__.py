# Layer 2: retrieval + explanation agent over recipe_database.json.
# Empty by design in Phase 1 (scaffolding + data contract only).
# Hard constraint (see ../../../README.md): this layer selects and explains
# recipes, it never computes or invents nutrition numbers. Implementation
# starts in Phase 5.
